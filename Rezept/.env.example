# mkdocsOnSteroids - Konfiguration
# Wird automatisch von bootstrap.sh generiert.
# Manuell anpassen und dann: ./bootstrap.sh start

# Pfad zum Quellcode (wird als /src in Docker gemountet, read-only)
SOURCE_DIR=/pfad/zum/quellcode

# Ausgabe-Verzeichnis fuer generierte Dokumentation
OUTPUT_DIR=./output

# OpenAI-kompatibler Server (Ollama, vLLM, LM Studio, etc.)
OPENAPI_SERVER_URL=http://host.docker.internal:11434

# API Key (leer = ohne Authentifizierung)
OPENAPI_KEY=

# MkDocs Server Port
SERVE_PORT=8000

# Ohne echten LLM-Server testen (true/false)
MOCK_MODE=false

# Parallele LLM-Anfragen
PARALLEL_WORKERS=3

# Nach Generierung automatisch MkDocs starten (true/false)
AUTO_SERVE=true

# Projektname (erscheint in der Dokumentation)
PROJECT_NAME=MyProject Documentation
