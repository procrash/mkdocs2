# mkdocsOnSteroids environment variables

# Path to source code to document (mounted as /src in Docker)
SOURCE_DIR=./

# Path for generated documentation output (mounted as /docs in Docker)
OUTPUT_DIR=./docs_project

# OpenAI-compatible server URL
OPENAPI_SERVER_URL=http://host.docker.internal:11434

# API key (leave empty for no auth)
OPENAPI_KEY=

# MkDocs serve port
SERVE_PORT=8000

# Set to true to run without a real LLM server
MOCK_MODE=false

# Number of parallel workers
PARALLEL_WORKERS=3

# Auto-serve documentation after generation
AUTO_SERVE=false

# Project name
PROJECT_NAME=MyProject Documentation
